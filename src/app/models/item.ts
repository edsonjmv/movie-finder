export class Item {
  constructor(
    public image: string,
    public subtitle: string,
    public title: string
  ) {
    this.image = image;
    this.subtitle = subtitle;
    this.title = title;
  }
}